namespace FA23_Convocation2023_API.Enums
{
    public enum AttendanceStatus
    {
        NotCheckedIn = 0,   // Not checked in
        Attended = 1,       // Attended
        Late = 2,           // Late
        Absent = 3          // Absent
    }
}